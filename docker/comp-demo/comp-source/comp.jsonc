// Descriptor for registering this folder as one new component version (pattern 1:
// the service builds the source). Register with:
//   python script/register_comp.py <this folder>
{
  "service": { "urlRegistry": "http://127.0.0.1:9415" },

  // resolved to compId via /api/comp/find-by-name; component is created when missing
  "compName": "hello-components",

  // version metadata, refer to /doc/service_resource.md#version-metadata
  "metadata": {
    "schemaVersion": 1,
    "versionName": "0.0.1",
    "description": "sample remote entry with a card and a badge",
    "exposeDefaultName": "hello-card",
    "federation": {
      "containerName": "helloCardApp",
      "fileEntry": "HelloCard.js"
    },
    "exposeList": [
      {
        "exposeName": "hello-card",
        "description": "card with a title and body text",
        "modulePath": "./hello-components",
        "fileEntrySource": "src/entry.jsx",
        "entryExport": "default",
        "props": {
          "data": { "type": "object", "description": "title and text to render" }
        },
        "packages": {
          "react": { "versionRequired": "^19.2.0", "isShared": true },
          "react-dom": { "versionRequired": "^19.2.0", "isShared": true }
        }
      },
      {
        "exposeName": "hello-badge",
        "description": "small status badge",
        "modulePath": "./hello-components",
        "fileEntrySource": "src/entry.jsx",
        "entryExport": "HelloBadge",
        "props": {
          "text": { "type": "string", "description": "badge text" },
          "tone": { "type": "string", "description": "info or success; optional" }
        },
        "packages": {
          "react": { "versionRequired": "^19.2.0", "isShared": true },
          "react-dom": { "versionRequired": "^19.2.0", "isShared": true }
        }
      }
    ]
  },

  // pattern 1: files under this folder are registered as sourceFileList
  "source": { "dir": "src" }
}
