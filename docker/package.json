{
  "name": "react-lazy-load-docker",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node ./src/server.js",
    "build:manage": "pnpm --filter react-lazy-load-manage-page run build"
  },
  "dependencies": {
    "@fastify/static": "^8.3.0",
    "@fastify/websocket": "^11.2.0",
    "fastify": "^5.2.1",
    "yaml": "^2.8.0"
  }
}
